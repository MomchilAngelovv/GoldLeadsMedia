@model GoldLeadsMedia.Web.Models.ViewModels.ManagersConfirmedLeadsViewModel

<h4 class="text-center">
    Select leads:
</h4>
<hr />

<table class="table table-sm">
    <tr class="row-header">
        <th>First name</th>
        <th>Last name</th>
        <th>Email</th>
        <th>PhoneNumber</th>
        <th>Country</th>
        <th>Offer</th>
        <th>Approved on</th>
        <th>Selected</th>
    </tr>
    <tbody class="table-tbody">

        @foreach (var data in Model.ConfirmedLeads)
        {
            <tr class="table-row clickable-table-row">
                <td>
                    @data.FirstName
                </td>
                <td>
                    @data.LastName
                </td>
                <td>
                    @data.Email
                </td>
                <td>
                    @data.PhoneNumber
                </td>
                <td>
                    @data.CountryName
                </td>
                <td>
                    @data.OfferName
                </td>
                <td>
                    @data.ApprovedOn
                </td>
                <td>
                    <input type="checkbox" name="lead-id" value="@data.Id">
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<div>
    <h4 class="text-center">
        Optional parameters:
    </h4>
    <div class="col-4">
        <label for="optional-offerId">Enter the offer Id given from partner</label>
        <input id="partner-offer-id" class="form-control" />
    </div>
</div>
<hr />
<div>
    <h4 class="text-center">
        Select partner:
    </h4>
    <div class="row">
        @foreach (var partner in Model.Partners)
        {
            <div class="col-2">
                <label>
                    <input type="radio" name="partner-id" value="@partner.Id"> @partner.Name
                </label>
            </div>
        }
    </div>
    <div>
        <button class="btn btn-primary" id="send-leads-btn">Send</button>
    </div>
</div>

@section Scripts
{
    <script src="~/js/managers/confirmed_leads.js"></script>
}

